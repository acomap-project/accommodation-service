name: CI

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  build-accom-listing-function:
    uses: ./.github/workflows/build-lambda-function.yaml
    with:
      name: accom-listing-function
      lambda-function-name: ACCOM_accom-listing-function
      region: ap-southeast-1
    secrets:
      aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      account-id: ${{ secrets.AWS_ACCOUNT_ID }}


  build-create-accom-function:
    uses: ./.github/workflows/build-lambda-function.yaml
    with:
      name: create-accom-function
      lambda-function-name: ACCOM_create-accom-function
      region: ap-southeast-1
    secrets:
      aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      account-id: ${{ secrets.AWS_ACCOUNT_ID }}